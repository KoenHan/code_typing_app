extends layout

block content
  h5.mb-0.ml-5 URL先のコードを使ってタイピング練習ができるサイトです．
  p.mb-2.ml-5 （GitHubのみ対応しています．）
  form(action="/gh_user" method="POST")
    .input-group.w-75
      |
      input.form-control.ml-7(
        type="search",
        name="user_name",
        placeholder="GitHubのユーザー名を入力してください．")
      |
      .input-group-append
        input.btn.btn-color(type="submit", value="検索")
    |
    if gh_user_se
      .text-danger.ml-4 #{gh_user_em}
  details.ml-10.mt-2.w-50
    summary.mb-1 githubユーザー名を用いた使い方
    img(data-gifffer="/images/explain/ghuser.gif", alt="githubユーザー名を用いた使い方.gif", height=500, width=800)
  br
  form(action="/gh_url" method="POST")
    .input-group.w-75
      |
      input.form-control.ml-7(
        type="search",
        name="url",
        list="samples",
        placeholder="URLを入力してください．")
      datalist(id="samples")
        option(value="sample.cpp")
        option(value="sample.py")
      |
      .input-group-append
        input.btn.btn-color(type="submit", value="練習する")
    |
    if gh_url_se
      .text-danger.ml-4 #{gh_url_em}
  details.ml-10.mt-2.w-50
    summary.mb-1 githubのURLを用いた使い方
    img(data-gifffer="/images/explain/url.gif", alt="githubのURLを用いた使い方.gif", height=500, width=800)
    p.mb-0 ※ファイルパスは https://github.com/.../file.extension のようにしてください．
    p.mb-0 ※試し打ち用に sample.cpp と sample.py があります．
  br
  form(action="/upload", method="POST", enctype="multipart/form-data")
    .input-group.w-75
      |
      input.form-control.ml-7(
        type="file",
        name="file",
        placeholder="ファイルをアップロードをしてください．")
      |
      .input-group-append
        input.btn.btn-color(type="submit", value="アップロード")
    |
    if upload_se
      .text-danger.ml-4 #{upload_em}
  details.ml-10.mt-2.w-50
    summary.mb-1 ファイルアップロードを用いた使い方
    img(data-gifffer="/images/explain/upload.gif", alt="ファイルアップロードを用いた使い方.gif", height=500, width=800)
  script(type="text/javascript", src="/javascripts/build/gifffer.min.js")
  script.
    window.onload = function() {
      Gifffer();
    }
